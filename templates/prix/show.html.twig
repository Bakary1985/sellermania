{% extends 'base.html.twig' %}

{% block title %}Prix{% endblock %}

{% block body %}
    <h1>Prix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prix.id }}</td>
            </tr>
            <tr>
                <th>PrixNormal</th>
                <td>{{ prix.prixNormal }}</td>
            </tr>
            <tr>
                <th>PrixPlancher</th>
                <td>{{ prix.prixPlancher }}</td>
            </tr>
            <tr>
                <th>IsPlancher</th>
                <td>{{ prix.isPlancher ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prix.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prix_index') }}">back to list</a>

    <a href="{{ path('prix_edit', {'id': prix.id}) }}">edit</a>

    {{ include('prix/_delete_form.html.twig') }}
{% endblock %}
