{% extends 'base.html.twig' %}

{% block title %}Concurrence{% endblock %}

{% block body %}
    <h1>Concurrence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concurrence.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ concurrence.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ concurrence.prix }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ concurrence.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('concurrence_index') }}">back to list</a>

    <a href="{{ path('concurrence_edit', {'id': concurrence.id}) }}">edit</a>

    {{ include('concurrence/_delete_form.html.twig') }}
{% endblock %}
