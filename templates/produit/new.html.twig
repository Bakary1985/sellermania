{% extends 'base.html.twig' %}

{% block title %}New Produit{% endblock %}

{% block body %}
    <h1>Create new Produit</h1>
    <ul>
        <p>Avant d'ajout un produit il faut ajouter d'abord ajouter :</p>
        <li> les Etats <a href="{{ path('etat_new') }}">Ajouter les Etats</a> </li>
        <li> les prix <a href="{{ path('prix_new') }}">Ajouter un prix</a> </li>
    </ul>
    {{ include('produit/_form.html.twig') }}

    <a href="{{ path('produit_index') }}">back to list</a>
{% endblock %}
